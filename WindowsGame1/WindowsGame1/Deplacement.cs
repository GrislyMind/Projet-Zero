using System;
using System.Collections.Generic;
using System.Linq;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Audio;
using Microsoft.Xna.Framework.Content;
using Microsoft.Xna.Framework.GamerServices;
using Microsoft.Xna.Framework.Graphics;
using Microsoft.Xna.Framework.Input;
using Microsoft.Xna.Framework.Media;

namespace WindowsGame1
{
    class Deplacement
    {
       /* private Vector2 pos0;
        private Vector2 casePosition;
        int caseLengthX;
        int caseLengthY;
        private MouseState _mouseState;

        public Vector2 GetcasePosition
        {
            get { return casePosition; }
            set { casePosition = value; }
        }

        public virtual void Update(GameTime gameTime)
        {
            _mouseState = Mouse.GetState();

            if (_mouseState.X > pos0.X && _mouseState.X < pos0.X + caseLengthX && _mouseState.Y > pos0.Y && _mouseState.Y < pos0.Y + caseLengthY && _mouseState.LeftButton == ButtonState.Pressed)
            {
                casePosition.X = _mouseState.X;
                casePosition.Y = _mouseState.Y;
            }
        }*/
    }
}
